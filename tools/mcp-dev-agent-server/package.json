{
  "name": "mcp-dev-agent-server",
  "version": "0.0.1",
  "private": true,
  "description": "Internal MCP server for repository development workflows",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p ./",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "smoke": "node scripts/smoke.mjs",
    "test:golden": "npm run build && node scripts/analyzer-golden.mjs",
    "install:repo": "node scripts/install-mcp-in-repo.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "typescript": "^5.6.2"
  }
}
